# Host-local extras (mold, sccache, cranelift) live in ~/.cargo/config.toml.
# Do not set jobs = 0 — Cargo 1.96+ rejects it; Cargo already uses all CPUs.
# Do not hard-code rustc-wrapper = "sccache" here — many machines lack it; CI
# sets RUSTC_WRAPPER in the job environment instead.
